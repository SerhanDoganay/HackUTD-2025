How to run:
Must have Node.js installed
Install all required libraries (many can be installed using the npm install from frontend folder)
1. Run analysis.py on a compiler
2. In a terminal, change directory to the frontend folder and do command npm run dev
3. Open the site in the localhost
